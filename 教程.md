# 新手游戏开发教程
### 适合对象：从未接触过游戏引擎的初学者 · 基于 Godot 4.x

---

## 目录

1. [认识 Godot 编辑器界面](#第一章认识-godot-编辑器界面)
2. [节点与场景基础操作](#第二章节点与场景基础操作)
3. [GDScript 核心语法](#第三章gdscript-核心语法)
4. [玩家角色场景](#第四章玩家角色场景-main_charactortscn)
5. [武器与子弹场景](#第五章武器与子弹场景)
6. [史莱姆敌人场景](#第六章史莱姆敌人场景-slimetscn)
7. [游戏主场景管理](#第七章游戏主场景管理-main_scenetscn)

---

## 第一章：认识 Godot 编辑器界面

### 1.1 创建项目

点击创建按钮，创建一个新项目。

<!-- 📷 图片：Godot 创建新项目的向导界面，标注项目名称、保存路径、项目类型 -->
![创建项目](images/ch1_new_project_1.png)  
*图 1-1：创建新项目向导*

![创建项目](images/ch1_new_project_2.png)  
*图 1-2：创建新项目界面*

- 更改项目名称为 `MyFirstGame`，或者任意你想要的名称
- 将项目路径改为便于找到的位置，如 `D:/GodotProjects/MyFirstGame`
- 选择 `Forward+` 渲染模式


### 1.2 整体布局

打开 Godot 后，你会看到如下界面分区：

<!-- 📷 图片：Godot 编辑器整体界面截图，标注各区域名称 -->
![Godot 编辑器整体界面](images/ch1_editor_overview.png)  
*图 1-3：Godot 编辑器整体布局*

| 区域 | 位置 | 用途 | 图中颜色
|------|------|------|------|
| **菜单栏** | 顶部 | 文件保存、项目设置、调试运行 | 黄色
| **工具栏** | 菜单栏下方 | 切换 2D/3D/脚本/资源视图 | 橙色
| **场景树（Scene）** | 左侧 | 显示当前场景所有节点，可增删改 | 红色
| **2D 视口** | 中央 | 可视化摆放节点，预览游戏画面 | 中间的主要界面
| **检视器（Inspector）** | 右侧 | 修改选中节点的所有属性 | 蓝色
| **文件系统** | 底部左侧 | 管理项目内所有文件和资源 | 绿色
| **输出日志** | 底部中央 | 查看运行时打印信息和报错 | 灰色
| **快捷操作按钮** | 顶部右侧 | 运行项目、快速测试 | 紫色

---

### 1.3 场景树面板

场景树是你在编辑器里使用最频繁的面板。

<!-- 📷 图片：场景树面板特写，标注"添加节点"按钮、节点右键菜单 -->
![场景树面板](images/ch1_scene_tree.png)  
*图 1-4：场景树面板*

**常用操作：**
- **添加节点**：点击场景树顶部的 `+` 图标
- **添加子节点**：选中节点后，点击场景树顶部的 `+` 图标，或按 `Ctrl+A`
- **重命名节点**：双击节点名称
- **删除节点**：选中后按 `Delete`
- **拖拽排序**：拖动节点可改变父子关系

---

### 1.4 检视器面板（Inspector）

选中任意节点后，右侧检视器会显示该节点的所有可调属性。

<!-- 📷 图片：选中 CharacterBody2D 后的检视器面板，展开 Transform / Collision 区域 -->
![检视器面板](images/ch1_inspector.png)  
*图 1-5：检视器面板示例（CharacterBody2D）*

> **💡 提示**：检视器中的属性分类折叠，点击分类名称可展开/收起。

---

### 1.5 文件系统面板

<!-- 📷 图片：文件系统面板，展示 scenes/ scripts/ assets/ 目录结构 -->
![文件系统面板](images/ch1_filesystem.png)  
*图 1-6：文件系统面板*

推荐将项目文件分为三大类存放：
- `scenes/` — `.tscn` 场景文件，每个游戏对象一个场景
- `scripts/` — `.gd` 脚本文件，控制节点行为的代码
- `assets/` — 图片、音效等美术资源

> **💡 提示**：可以为美术资源创建子目录，如 `assets/sprites/`、`assets/sounds/` 等。

---

### 1.6 运行游戏

<!-- 📷 图片：顶部运行按钮区域特写，标注"运行项目"和"运行当前场景"按钮 -->
![运行按钮](images/ch1_run_buttons.png)  
*图 1-7：运行游戏的按钮*

| 快捷键 | 功能 |
|--------|------|
| `F5` | 运行整个项目（从主场景开始） |
| `F6` | 运行当前打开的场景 |
| `F8` | 停止运行 |

---

### 1.7 打开项目设置

项目设置包含全局配置，如碰撞层命名、主场景指定等。建议在开始制作任何场景之前先完成基础配置。

<!-- 📷 图片：顶部菜单 Project 展开，鼠标悬停在 Project Settings 选项上 -->
![打开项目设置](images/ch1_project_settings_menu.png)
*图 1-8：通过顶部菜单打开项目设置*

在编辑器顶部菜单栏点击 `项目` → `项目设置`，即可打开项目设置窗口。本教程中会在需要配置的地方告知你进入哪个设置页面。

---

### 1.8 命名物理碰撞层

Godot 默认用数字区分物理层，命名后检视器中会显示层名（而非 Layer 1、Layer 2），方便后续章节设置碰撞层时识别。

<!-- 📷 图片：Project Settings 左侧搜索 Layer Names，展开2D Physics，显示各层命名输入框 -->
![碰撞层命名](images/ch1_collision_layers.png)
*图 1-9：在项目设置中为碰撞层命名，后续所有场景都会使用这套分层规则*

**操作步骤：**
1. 打开 `项目设置`（见上方 1.7）
2. 在左侧搜索框搜索 `2D物理`，点击展开
3. 按照下表依次填入层名：

| 层编号 | 名称 | 用途 |
|--------|------|------|
| Layer 1 | `world` | 地图 / 墙壁（TileMapLayer） |
| Layer 2 | `player` | 玩家角色（CharacterBody2D） |
| Layer 3 | `slime` | 史莱姆敌人 |
| Layer 4 | `bullet` | 子弹（Area2D） |

> **💡 提示**：命名完成后，后续章节在检视器中设置 `Collision Layer` 时，就会显示你填写的名称，而不是数字。

> **💡 说明**: 物理层命名只是为了方便识别，对实际运行并没有影响。

---

## 第二章：节点与场景基础操作

### 2.1 什么是节点（Node）

节点是 Godot 最基本的构建单元。**每个节点只做一件事**：

| 节点类型 | 职责 |
|----------|------|
| `AnimatedSprite2D` | 显示并播放帧动画 |
| `CollisionShape2D` | 定义物体的碰撞外形 |
| `Area2D` | 检测与其他物体的区域重叠 |
| `CharacterBody2D` | 提供角色物理移动能力 |
| `AudioStreamPlayer` | 播放音效 |
| `RayCast2D` | 向某方向发射射线，检测前方物体 |
| `CanvasLayer` | 独立于游戏世界的 UI 层（不随摄像机移动） |
| `TileMapLayer` | 用瓦片绘制地图（地面、墙壁） |
| `...` | ... |

---

### 2.2 创建新场景

<!-- 📷 图片：Scene 菜单展开，鼠标悬停在"New Scene"选项上 -->
![新建场景](images/ch2_new_scene.png)  
*图 2-1：新建场景*

**步骤：**
1. 顶部工作区域的 `[空]` ，或点击右侧 `+` 图标创建新的场景
2. 在左侧场景树中点击 `+` 添加根节点，选择节点类型
3. 按 `Ctrl+S` 保存，文件名建议与功能对应（如 `player.tscn`）

---

### 2.3 向场景添加节点

<!-- 📷 图片：点击"+"后弹出的节点选择对话框，搜索框中输入"Animated" -->
![添加节点对话框](images/ch2_add_node_dialog.png)  
*图 2-2：添加节点对话框，可搜索节点类型*

**步骤：**
1. 添加根节点 -> 按 `Ctrl+A` 或点击场景树上的 `+` 图标
2. 添加子节点 -> 在场景树中**先选中父节点** -> 重复步骤1
3. 在搜索框输入节点名称关键词，双击选中类型 `or` 在左侧收藏或最近使用列表中选择
4. 新节点出现在场景树中或父节点下方

> **💡 建议**：将所有场景名用大写英文首字母+小写英文全拼命名，且使用有意义的名称，不要使用下划线，与文件命名分开。例如：文件名`main_charactor.tscn`，场景名`MainCharactor`。
---

### 2.4 为节点附加脚本

<!-- 📷 图片：选中根节点，右键菜单中"Attach Script"选项高亮 -->
![附加脚本](images/ch2_attach_script.png)  
*图 2-3：右键节点选择"添加脚本"(记得重命名根节点为有意义的名称)*

<!-- 📷 图片：创建脚本对话框，显示路径、语言、模板选项 -->
![创建脚本对话框](images/ch2_create_script_dialog.png)  
*图 2-4：创建脚本对话框*

**步骤：**
1. 在场景树中选中要附加脚本的节点（通常是根节点）
2. 右键 → `创建脚本`
3. 设置脚本保存路径（建议放到 `scripts/` 目录），更改脚本名称
4. 点击 `创建` 完成

> **💡 提示**：脚本图标会出现在节点名称旁边，点击可跳转到代码编辑器。

> **💡 建议**：将所有保存的文件用小写英文加下划线`_`命名，且使用有意义的名称，方便管理。如：`main_charactor.gd`、`slime.tscn` 等，不要使用大写英文。
---

## 第三章：GDScript 核心语法

> 本章只讲本项目用到的语法，作为后续章节的参考手册。  
> 请务必保证输入代码时使用的是英文输入法，避免出现乱码问题。  
> 本章适合具有一定其他编程语言基础的读者大概了解 GDScript 语法。  

### 3.1 变量与常量

```gdscript
var hp = 100           # var：运行中会变化的值，用于设定变量
var speed: float = 150.0  # 可以在:后指定类型 float（小数）
const MAX_HP = 100      # const：永远不变的值（常量）
```

> `=` 或者 `:=` 让 Godot 自动判断类型；`const` 表示永远不变的值（常量），通常用于最大血量、速度上限等不变数值。

---

### 3.2 Godot 自动调用的生命周期函数

| 函数 | 触发时机 | 典型用途 |
|------|----------|----------|
| `_ready()` | 节点进入场景树时，执行**一次** | 初始化变量、连接信号 |
| `_process(delta)` | 每帧执行（约 60次/秒） | UI 更新、输入轮询 |
| `_physics_process(delta)` | 每物理帧执行（固定频率） | 移动、碰撞检测 |
| `_input(event)` | 有键盘/鼠标事件时执行 | 单次按键触发（如射击） |
| `_unhandled_input(event)` | 未被 UI 拦截的输入事件 | 暂停键等全局热键 |

```gdscript
# delta 的意义：让移动速度与帧率无关
# 无论 30fps 还是 60fps，角色每秒移动距离相同
position += direction * SPEED * delta
```

---

### 3.3 信号（Signal）——节点间通信

信号是 Godot 的"广播"机制，让不同节点之间松散地联系：

```gdscript
# ① 定义信号（在发送方脚本顶部）
signal died

# ② 发出信号（在某个条件满足时）
died.emit()

# ③ 在另一个节点中监听信号（通常在 _ready 中连接）
$MainCharactor.died.connect(_on_player_died)

# ④ 信号触发时执行的函数
func _on_player_died() -> void:
    $DeathScreen.show()
```

> **好处**：玩家脚本只管"我死了"，不需要知道死后发生什么；场景脚本负责响应。两者代码互不干扰。

---

### 3.4 向量 Vector2

2D 游戏中坐标和方向都用 `Vector2(x, y)` 表示：

```gdscript
Vector2(100, 0)    # 向右移动
Vector2(0, 100)    # 向下移动（Godot Y 轴朝下）
Vector2.ZERO       # 静止 (0, 0)

direction.normalized()       # 只保留方向，长度变为 1（防止对角线过快）
a.distance_to(b)             # 两点之间的距离
velocity.lerp(target, 0.1)   # 平滑过渡：每帧向 target 靠近 10%
```

---

### 3.5 节点引用

```gdscript
$AnimatedSprite2D          # 引用名为 AnimatedSprite2D 的直接子节点
$GunPivot/Muzzle           # 用 / 访问更深层的子节点
get_tree().get_nodes_in_group("slimes")  # 获取场景中所有在 slimes 组的节点
```

---

## 第四章：玩家角色场景（main_character.tscn）

> 本章将从一个**完全空白的场景文件**出发，每一节只添加一个节点，  
> 你可以清楚看到场景树是如何一步步从空白变为完整的玩家角色场景的。

**本章结束后，你将拥有如下完整节点树：**

```
MainCharacter（CharacterBody2D）  ← 根节点，控制物理移动
├── AnimatedSprite2D              ← 显示角色动画
├── CollisionShape2D              ← 角色的碰撞体（矩形）
├── Hitbox（Area2D）              ← 翻滚攻击的检测区域
│     └── CollisionShape2D       ← 翻滚攻击碰撞形状
├── HurtSound（AudioStreamPlayer）← 受伤音效
└── GunPivot（Node2D）            ← 枪的旋转轴心
      ├── handgun（实例）         ← 手枪外观（子场景）
      └── Muzzle（Node2D）        ← 枪口位置标记（子弹从这里生成）
```

---

### 4.1 创建空白场景文件

**当前场景树状态：**


<!-- 📷 图片：Godot 编辑器中新建场景后，场景树面板完全空白的状态 -->
![空白场景树](images/ch4_empty_scene.png)  
*图 4-1：新建场景后，场景树为完全空白状态*

**操作步骤：**
1. 点击顶部工作区标签栏右侧的 `+` 图标，或菜单 `场景` -> `新建场景`
2. 此时左侧场景树面板为空，没有任何节点
3. 按 `Ctrl+S` 保存场景，命名为 `main_character.tscn`，保存到 `scenes/` 目录

> **💡 提示**：养成先保存空场景再添加节点的好习惯，避免未保存时意外丢失工作。

---

### 4.2 步骤一：添加根节点 CharacterBody2D

**当前场景树状态：**


<!-- 📷 图片：场景树中只有根节点 MainCharactor（CharacterBody2D），节点名称已重命名 -->
![添加根节点](images/ch4_step1_root.png)  
*图 4-2：场景树中添加了根节点 CharacterBody2D，并重命名为 MainCharacter*

**为什么选 CharacterBody2D？**

`CharacterBody2D` 是专门为"可控制角色"设计的节点，它内置了 `move_and_slide()` 函数，可以在移动时自动处理与墙壁、地面的碰撞，不需要你手动写碰撞检测逻辑。

**操作步骤：**
1. 在空白场景树中点击 `+` 图标，搜索并添加 `CharacterBody2D`
2. 双击节点名称，将其重命名为 `MainCharactor`（注意：根节点名称即场景名称）
3. 按 `Ctrl+S` 保存

**在检视器（Inspector）中设置以下属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Collision Layer` | 第 2 层（player层） | 该节点"属于"哪个物理层，其他物体用此判断它是谁 |
| `Collision Mask` | 第 1 层（world层） | 该节点"检测"哪些层，设置为地图/墙壁所在层 |


<!-- 📷 图片：选中根节点 MainCharactor，检视器中展开 Collision 区域，显示 Layer 和 Mask 的勾选状态 -->
![CharacterBody2D 检视器](images/ch4_charbody_inspector.png)  
*图 4-3：CharacterBody2D 的碰撞层与运动模式设置*

> **💡 说明**：碰撞层已在第一章 1.8 节统一命名，这里只需将玩家放第 2 层（player）、地图放第 1 层（world）即可。

---

### 4.3 步骤二：添加 AnimatedSprite2D（角色外观与动画）

**当前场景树状态：**

<!-- 📷 图片：场景树中 MainCharactor 下出现 AnimatedSprite2D 子节点，2D 视口中角色贴图出现 -->
![添加 AnimatedSprite2D](images/ch4_step2_sprite.png)  
*图 4-4：添加 AnimatedSprite2D 后，场景树更新*

**为什么选 AnimatedSprite2D？**

`AnimatedSprite2D` 可以管理多组帧动画（如站立、行走、死亡等），只需调用 `play("动画名")` 即可切换播放，非常适合控制动画的播放。

**操作步骤：**
1. 选中 `MainCharactor` 根节点
2. 按 `Ctrl+A`，搜索 `AnimatedSprite2D`，双击添加
3. 在检视器中找到 `Sprite Frames` 属性，点击 `[空]` → `新建 SpriteFrames`
4. 点击检视器底部弹出的动画编辑器，依次创建以下动画并导入对应帧图片

<!-- 📷 图片：选中 AnimatedSprite2D，检视器中 SpriteFrames 属性旁有"Open Editor"按钮 -->
![动画帧编辑器入口](images/ch4_sprite_frames_entry.png)  
*图 4-5：点击 Sprite Frames，新建SpriteFrames*

单击新建的 `SpriteFrames` ，进入底部的动画帧编辑器

<!-- 📷 图片：底部动画帧编辑器，左侧列出 idle / move / dash / injury / death 等动画名称 -->
![动画帧编辑器](images/ch4_sprite_frames_editor.png)  
*图 4-6：在动画编辑器中创建五个动画，名称必须与代码完全一致，将idle动画设为默认播放动画*

| 动画名 | 触发条件 | 是否循环 |
|--------|----------|---------|
| `idle` | 站立不动 | ✅ 循环 |
| `move` | 正在移动 | ✅ 循环 |
| `dash` | 翻滚冲刺中 | ✅ 循环 |
| `injury` | 受伤后短暂时间内 | ✅ 循环 |
| `death` | 血量归零 | ❌ **不循环** |

> **💡 重要**：`death` 动画必须关闭循环（在动画编辑器中点击循环图标取消高亮），否则死亡动画播完后会重新播放，死亡逻辑可能出现问题。

![动画帧编辑器](images/ch4_sprite_frames_death.png)  
*图 4-7：`death` 动画必须关闭循环*

**动画帧的添加：**

1. 创建新动画，为其命名；
2. 选中该动画，按下 `ctrl+shift+O` 添加帧图片；
3. 在文件夹中找到对应的精灵表图片并打开；
4. 选择这个动画需要的帧图片添加到动画中；
5. 设置动画的属性，并按下 `D` 播放动画，检查帧是否添加正确。

![动画帧编辑器](images/ch4_animation_frames.png)  
*图 4-8：动画帧编辑器中为动画选择精灵表*

![动画帧编辑器](images/ch4_select_frames.png)  
*图 4-9：动画帧编辑器中选择动画帧*

**如何选择动画帧**

> **水平/垂直**两个数据，分别可以更改在水平/垂直方向上将图片分割成多少份  
> **大小**属性会更改每个格子的大小   
> **间距**属性会分别更改水平/垂直方向上每个格子间的间距  
> **偏移**属性会在不影响总体大小的前提下，整体移动图片  

当你将该精灵图裁剪为需要的属性后，即可在动画编辑器中点击帧图片添加到动画中。

> 也许你已经注意到了，现在添加的动画并不是像素的，而是十分模糊的。
> 这是因为godot会自动进行纹理平滑处理，使纹理看起来更模糊。

![纹理平滑处理](images/ch4_texure_filter.png)   
*图 4-10：更改纹理过滤模式*

**设置纹理过滤器：**

1. 打开`项目设置`，搜索`纹理`
2. 将`默认纹理过滤`从`Linear`设置为`Nearest`

![动画帧率设置](images/ch4_fps.png)
*图 4-11：设置动画帧率*

**更改动画速度：**

> 在这个动画编辑器中，还有一个`动画速度`属性，可以更改动画的播放速度。  
> 在`自动播放`按钮边上，有一个更改fps的属性，可以更改动画的帧率。  

> 该属性的数值决定了你的游戏每秒会为这个动画播放多少帧。
> 数值越大，每秒播放的帧数越多，动画播放速度越快。  
> 通常来讲，角色动画的帧率并没有定值，它取决于动画的帧数与你希望的播放速度。  
> 所以，你可以根据需要调整帧率，以实现你希望的动画播放速度。  

**以下是参考的动画设置：**
![动画设置](images/ch4_animation_dash.png)
![动画设置](images/ch4_animation_death.png)
![动画设置](images/ch4_animation_idle.png)
![动画设置](images/ch4_animation_injury.png)
![动画设置](images/ch4_animation_move.png)


### 4.4 步骤三：添加 CollisionShape2D（物理碰撞形状）

**当前场景树状态：**

<!-- 📷 图片：选中 CollisionShape2D，2D 视口中出现蓝色矩形碰撞框，与角色图像贴合 -->
![玩家碰撞框](images/ch4_collision_shape.png)  
*图 4-8：设置碰撞形状*

**为什么需要 CollisionShape2D？**

`CharacterBody2D` 本身没有形状，必须给它添加一个 `CollisionShape2D` 子节点来告诉 Godot"角色的身体是什么形状"。没有碰撞形状，角色会直接穿过所有墙壁。

**操作步骤：**
1. 选中 `MainCharactor` 根节点，按 `Ctrl+A` 添加 `CollisionShape2D`
2. 在检视器中点击 `Shape` 属性旁的下拉菜单，选择 `新建 RectangleShape2D`
3. 在 2D 视口中拖动碰撞框的边角控制点，让矩形框与角色图像贴合

![创建碰撞箱](images/ch4_shape.png)  
*图 4-12：创建角色碰撞箱（建议选用矩形或者圆形）*

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Shape` | `RectangleShape2D` | 矩形碰撞体，适合人形角色 |
| `Shape → Size` | 约 `(8, 7.25)` | 宽和高（像素），根据角色实际大小调整 |
| `Disabled` | `false`（默认） | 碰撞默认开启，死亡时代码会将其设为 true |

> **💡 技巧**：碰撞框建议比角色图像略小一点（内缩 2~3 像素），并根据游戏视角调整大小，游戏手感会更宽松自然。

![碰撞框调整](images/ch4_shape_adjust.png)  
*图 4-13：调整碰撞框大小(以idle动画为模板)*

---

### 4.5 步骤四：添加 Hitbox（翻滚攻击检测区域）

**当前场景树状态：**

<!-- 📷 图片：场景树中 Hitbox（Area2D）节点下有一个 CollisionShape2D 子节点 -->
![Hitbox 节点树](images/ch4_hitbox_tree.png)   
*图 4-14：Hitbox 是一个 Area2D，它也需要自己的 CollisionShape2D 来定义检测范围*

**为什么需要 Hitbox？**

玩家翻滚冲刺时可以攻击史莱姆。`Area2D` 能检测"是否有其他物体进入我的范围"，非常适合做攻击判定。`Area2D`还可以单独控制物理层的碰撞，与物理碰撞分离。

**操作步骤：**
1. 选中 `MainCharactor` 根节点，按 `Ctrl+A` 添加 `Area2D`，重命名为 `Hitbox`
2. 选中刚添加的 `Hitbox`，按 `Ctrl+A` 为其添加 `CollisionShape2D` 子节点
3. 为这个 `CollisionShape2D` 设置形状（同样使用 `RectangleShape2D`，大小与角色碰撞箱可以不同）

<!-- 📷 图片：选中 Hitbox 节点，检视器中 Monitoring = false，Monitorable = true，2D 视口中显示 Hitbox 的橙色碰撞范围 -->
![Hitbox 检视器](images/ch4_hitbox_inspector.png)   
*图 4-15：Hitbox 碰撞箱设置和检查器设置*

**检视器关键属性（选中 Hitbox 节点时设置）：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Monitoring` | `false`（关闭） | 是否主动检测进入范围的其他物体；翻滚时由代码开启 |
| `Monitorable` | `true`（开启） | 是否允许被其他 Area2D 检测到（子弹需要检测到它） |
| `Collision Layer` | 不需要设置 | Hitbox 不"属于"任何层 |
| `Collision Mask` | 史莱姆所在层 | 只检测史莱姆，避免误触玩家自身 |

---

### 4.6 步骤五：添加 HurtSound（受伤音效）

**当前场景树状态：**


<!-- 📷 图片：场景树中 HurtSound（AudioStreamPlayer）节点出现，检视器中 Stream 属性指向音效文件 -->
![HurtSound 节点](images/ch4_hurtsound.png)   
*图 4-16：新增AudioStreamPlayer 节点，重命名为HurtSound*

**为什么需要 AudioStreamPlayer？**

`AudioStreamPlayer` 是 Godot 中播放音效的标准节点，调用 `play()` 即可播放。将它挂在玩家节点下，受伤时代码直接 `$HurtSound.play()` 即可发声，不需要额外管理音效生命周期。

**操作步骤：**
1. 选中 `MainCharactor` 根节点，按 `Ctrl+A` 添加 `AudioStreamPlayer`，重命名为 `HurtSound`
2. 在检视器中点击 `Stream` 属性旁的下拉框，选择 `快速加载`，找到受伤音效文件（`hurt.wav` ）

![HurtSound 节点](images/ch4_load_sound.png)   
*图 4-17：更改 HurtSound 的 Stream 属性*

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Stream` | 受伤音效文件 | 拖入或选择音频资源文件 |
| `Volume Db` | `0.0` | 音量（分贝），0 为原始音量，负数更安静 |
| `Autoplay` | `false` | 不自动播放，由代码在受伤时调用 `play()` |

---

### 4.7 完整场景确认（第五章前）

完成以上步骤后，你的场景树应如下所示（共 6 个节点）：

<!-- 📷 图片：Godot 编辑器中打开 main_charactor.tscn，场景树展开全部节点，与文字结构完全对应 -->
![玩家完整场景树](images/ch4_player_scene_tree_final.png)  
*图 4-17：第五章前的玩家角色节点树，共 6 个节点*


> **💡 说明**：`GunPivot`（枪械系统）需要先完成第五章的手枪场景（`handgun.tscn`）再安装，详见 **5.10 节**。

> 按 `Ctrl+S` 保存场景后，进入下一节为场景附加脚本。

---

### 4.8 配置输入映射（Input Map）

在为玩家编写移动脚本之前，需要先在项目设置中定义"动作（Action）"——即把按键绑定到有意义的名称上。代码中用 `Input.get_axis("move_left", "move_right")` 读取输入，这些名称必须与 Input Map 中**完全一致**。

<!-- 📷 图片：Project Settings 窗口切换到 Input Map 标签页，显示已有的动作列表 -->
![Input Map 列表](images/ch4_input_map.png)  
*图 4-17：项目设置中的输入映射界面*

<!-- 📷 图片：点击某个动作右侧的"+"按钮后，弹出按键录制对话框 -->
![添加按键绑定](images/ch4_add_action.png)  
*图 4-18：输入动作名后点击添加添加动作*

![添加按键绑定](images/ch4_add_key_binding.png)  
*图 4-19：在对应动作名下添加按键映射*

**操作步骤：**
1. 菜单栏点击 `项目` → `项目设置`，切换到 `输入映射` 标签页
2. 在顶部 `添加新动作` 输入框输入动作名（如 `move_up`），点击 `+添加`
3. 新动作出现在列表中，点击其右侧 `+` 图标
4. 在弹出的对话框中**按下目标键**（如 `W`），完成绑定

本项目需要配置以下所有动作：

| 动作名 | 绑定按键 | 用途 |
|--------|----------|------|
| `move_up` | `W` | 向上移动 |
| `move_down` | `S` | 向下移动 |
| `move_left` | `A` | 向左移动 |
| `move_right` | `D` | 向右移动 |
| `dash` | `Shift` | 翻滚冲刺 |
| `pause` | `Escape` | 暂停/继续 |
| `reset` | `R` | 长按重置游戏 |

> **💡 说明**：`pause` 和 `reset` 在第七章主场景脚本中使用，这里一并配置好，避免后续遗漏。

---

### 4.9 附加脚本（main_character.gd）函数说明

**操作步骤：**
1. 选中根节点 `MainCharacter`
2. 右键 → `创建脚本`，保存路径设为 `scripts/main_character.gd`
3. 点击 `创建`，脚本编辑器自动打开

```gdscript
extends CharacterBody2D
```
继承 `CharacterBody2D`，使脚本获得 `move_and_slide()` 等物理移动能力。

---

#### `_ready()` — 初始化

```gdscript
func _ready() -> void:
    add_to_group("player")       # 加入 "player" 组，便于其他脚本通过组名找到玩家
    health_bar = ProgressBar.new()  # 用代码动态创建血条节点（无需在编辑器手动添加）
    health_bar.position = Vector2(-16, -22)  # 血条显示在角色头顶
    add_child(health_bar)        # 将血条加入自身子节点
```

---

#### `_physics_process(delta)` — 每物理帧：移动与冲刺

```gdscript
func _physics_process(delta: float) -> void:
    # 读取 WASD 输入，合成方向向量（x: -1~1, y: -1~1）
    var direction := Vector2(
        Input.get_axis("move_left", "move_right"),
        Input.get_axis("move_up", "move_down")
    )
    if direction != Vector2.ZERO:
        velocity = direction.normalized() * SPEED
    else:
        velocity = Vector2.ZERO
    move_and_slide()   # 自动处理碰撞，更新 position
```

> `move_and_slide()` 是 Godot 内置函数，根据 `velocity` 移动角色并自动滑动绕过障碍物边角。

---

#### `take_damage(amount)` — 受到伤害

```gdscript
func take_damage(amount: int) -> void:
    if is_dashing: return      # 翻滚期间无敌，直接忽略伤害
    if is_dead or hp <= 0: return  # 已死亡则跳过
    hp -= amount               # 扣除血量
    hp = max(0, hp)            # 血量最低为 0，不能为负
    health_bar.value = hp      # 更新血条显示
    $HurtSound.play()          # 播放受伤音效
    is_injured = true          # 触发受伤动画和短暂无敌帧
    if hp <= 0:
        _die()
```

---

#### `_die()` — 死亡处理

```gdscript
func _die() -> void:
    is_dead = true
    velocity = Vector2.ZERO
    $CollisionShape2D.set_deferred("disabled", true)  # 禁用碰撞（deferred：下帧执行，物理安全）
    anim.play("death")
    await anim.animation_finished  # 暂停执行，等待动画播放完毕
    if is_inside_tree():           # 确认节点还在场景中（防止场景被重载时报错）
        died.emit()                # 发出死亡信号 → main_scene 收到后显示死亡界面
```

> `await` 是 GDScript 的协程关键字，让函数“暂停在这里等待”，不会阻塞游戏主线程。

---

> **💡 枪械控制相关脚本说明**：`_shoot()` 函数以及玩家脚本中需要添加的鼠标跟随旋转、射击按键等枪械逻辑，将在完成第五章后的 **5.11 节** 统一讲解。

---

# 第五章：武器与子弹场景

> 本章包含两个独立场景：`handgun.tscn`（手枪外观）和 `bullet.tscn`（子弹逻辑），同样从空白出发，逐步添加节点。

**本章结束后，你将拥有两个完整的武器场景：**

```
handgun（Node2D）
└── AnimatedSprite2D           ← 手枪贴图
```
```
bullet（Area2D）
├── AnimatedSprite2D           ← 子弹外观
├── CollisionShape2D           ← 子弹碰撞体
└── RayCast2D                  ← 前方墙体检测射线
```

---

## 一、手枪场景（handgun.tscn）

### 5.1 创建空白场景文件（handgun.tscn）

**当前场景树状态：**
```
（空）
```

**操作步骤：**
1. 点击工作区标签栏的 `+` 新建场景
2. 按 `Ctrl+S` 保存为 `handgun.tscn`，保存到 `scenes/` 目录

---

### 5.2 步骤一：添加根节点 Node2D

**当前场景树状态：**
```
handgun（Node2D）    ← ✅ 刚刚添加
```

<!-- 📷 图片：场景树中只有根节点 handgun（Node2D），已重命名 -->
![手枪根节点](images/ch5_handgun_root.png)
*图 5-1：手枪场景根节点选用 Node2D，不需要物理能力*

**为什么选 Node2D？**

手枪本身不需要物理碰撞，只需要跟随 `GunPivot` 旋转显示外观，选最简单的 `Node2D` 作根节点，避免引入不必要的物理开销。

**操作步骤：**
1. 在空白场景树中点击 `+`，搜索并添加 `Node2D`
2. 双击节点名称，重命名为 `handgun`
3. 按 `Ctrl+S` 保存

---

### 5.3 步骤二：添加 AnimatedSprite2D（手枪外观）

**当前场景树状态：**
```
handgun（Node2D）
└── AnimatedSprite2D    ← ✅ 刚刚添加
```

<!-- 📷 图片：场景树中 handgun 下出现 AnimatedSprite2D，2D 视口中显示手枪贴图 -->
![手枪外观节点](images/ch5_handgun_sprite.png)
*图 5-2：添加 AnimatedSprite2D 后手枪贴图出现在视口中*

**操作步骤：**
1. 选中 `handgun` 根节点，按 `Ctrl+A` 添加 `AnimatedSprite2D`
2. 在检视器中点击 `Sprite Frames` → `新建 SpriteFrames`，导入手枪贴图帧

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Sprite Frames` | 新建并导入手枪图片 | 可以只有一帧静态图 |
| `Position` | 调整至枪柄靠近原点 | `GunPivot` 旋转以原点为轴，枪柄应在原点附近 |

> **💡 提示**：手枪图片的枪柄端应尽量靠近节点原点（视口中的黄色 + 号），这样旋转时才会以握枪手部为轴心旋转。

---

### 5.4 handgun.tscn 完整确认

<!-- 📷 图片：完整的 handgun.tscn 场景树，包含 handgun（Node2D）和 AnimatedSprite2D -->
![手枪完整场景树](images/ch5_handgun_tree.png)
*图 5-3：handgun.tscn 最终节点树，共 2 个节点*

```
handgun（Node2D）              ← 根节点，不需要物理能力
└── AnimatedSprite2D           ← 手枪外观贴图
```

> 保存后，该场景在第四章中会被拖入玩家场景的 `GunPivot` 节点下作为实例使用。

---

## 二、子弹场景（bullet.tscn）

### 5.5 创建空白场景文件（bullet.tscn）

**当前场景树状态：**
```
（空）
```

**操作步骤：**
1. 点击工作区标签栏的 `+` 新建场景
2. 按 `Ctrl+S` 保存为 `bullet.tscn`，保存到 `scenes/` 目录

---

### 5.6 步骤一：添加根节点 Area2D

**当前场景树状态：**
```
bullet（Area2D）    ← ✅ 刚刚添加
```

<!-- 📷 图片：场景树中只有根节点 bullet（Area2D），已重命名 -->
![子弹根节点](images/ch5_bullet_root.png)
*图 5-4：子弹根节点选用 Area2D，专门用于检测与史莱姆 Hitbox 的重叠*

**为什么选 Area2D？**

子弹需要检测"是否飞入了史莱姆的 Hitbox 范围"，`Area2D` 专门用于区域重叠检测。当子弹（Area2D）和史莱姆的 Hitbox（也是 Area2D）重叠时，会触发 `area_entered` 信号，代码据此扣除伤害。

**操作步骤：**
1. 在空白场景树中点击 `+`，添加 `Area2D`
2. 重命名为 `bullet`
3. 按 `Ctrl+S` 保存

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Collision Layer` | 第 4 层（bullet） | 子弹"属于"bullet层 |
| `Collision Mask` | 第 3 层（slime） | 只检测史莱姆层的 Hitbox，不误触其他物体 |

---

### 5.7 步骤二：添加 AnimatedSprite2D（子弹外观）

**当前场景树状态：**
```
bullet（Area2D）
└── AnimatedSprite2D    ← ✅ 刚刚添加
```

<!-- 📷 图片：场景树 bullet 下出现 AnimatedSprite2D，2D 视口中显示子弹贴图 -->
![子弹外观](images/ch5_bullet_sprite.png)
*图 5-5：添加 AnimatedSprite2D 后子弹贴图出现在视口中*

**操作步骤：**
1. 选中 `bullet` 根节点，按 `Ctrl+A` 添加 `AnimatedSprite2D`
2. 导入子弹贴图（通常是小亮点或细长形状）

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Sprite Frames` | 导入子弹图片 | 可以是静态一帧，也可以有飞行动画 |
| `Playing` | `true` | 如有动画则自动播放 |

---

### 5.8 步骤三：添加 CollisionShape2D（子弹碰撞体）

**当前场景树状态：**
```
bullet（Area2D）
├── AnimatedSprite2D
└── CollisionShape2D    ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 CollisionShape2D，2D 视口中显示细长碰撞框覆盖在子弹上 -->
![子弹碰撞体](images/ch5_bullet_collision.png)
*图 5-6：子弹碰撞体使用细长形状，沿飞行方向拉长*

**为什么需要 CollisionShape2D？**

`Area2D` 同样需要 `CollisionShape2D` 来定义检测范围的形状。子弹使用细长的碰撞形状，沿飞行方向延伸，这样飞行时不易漏检。

**操作步骤：**
1. 选中 `bullet` 根节点，按 `Ctrl+A` 添加 `CollisionShape2D`
2. 在检视器中选择 `CapsuleShape2D`，调整为细长形状

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Shape` | `CapsuleShape2D` | 胶囊形，比矩形更平滑 |
| `Shape → Radius` | `2` | 碰撞半径（像素） |
| `Shape → Height` | `12` | 碰撞长度，沿子弹飞行方向 |
| `Rotation` | `90°` | 将胶囊旋转为水平，使其沿 X 方向（飞行方向）延伸 |

---

### 5.9 步骤四：添加 RayCast2D（墙体穿透检测）

**当前场景树状态：**
```
bullet（Area2D）
├── AnimatedSprite2D
├── CollisionShape2D
└── RayCast2D           ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 RayCast2D，2D 视口中显示绿色射线箭头沿子弹正前方延伸，检视器展示 Target Position 属性 -->
![RayCast2D 设置](images/ch5_raycast_inspector.png)
*图 5-7：RayCast2D 绿色箭头指向子弹飞行方向，用于提前检测前方墙体*

**为什么需要 RayCast2D？**

`Area2D` 的碰撞检测在每帧末才更新，高速子弹可能一帧内"穿过"薄墙。`RayCast2D` 在每个物理帧都向前探测，只要前方有墙就立刻销毁子弹，彻底防止穿墙。

**操作步骤：**
1. 选中 `bullet` 根节点，按 `Ctrl+A` 添加 `RayCast2D`
2. 在检视器中设置以下属性

**检视器关键属性：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Target Position` | `Vector2(20, 0)` | 射线长度和方向（沿本地 X 轴正方向，即子弹飞行方向） |
| `Collision Mask` | 第 1 层（world） | 只检测地图/墙壁层 |
| `Collide With Bodies` | `true` | 检测 TileMapLayer 等物理体（墙壁） |
| `Collide With Areas` | `false` | 不检测 Area2D，避免误触 |
| `Enabled` | `true` | 勾选后射线才会生效 |

> **💡 说明**：由于子弹的 `rotation` 与飞行方向一致，`RayCast2D` 的本地 +X 方向就是子弹飞行方向，无需额外旋转。

---

## 三、将手枪安装到玩家场景（返回 main_charactor.tscn）

> `handgun.tscn` 已制作完成，现在可以返回玩家场景，将手枪安装到角色身上。

### 5.10 步骤五：添加 GunPivot（枪械旋转系统）

**当前场景树状态（main_charactor.tscn）：**
```
MainCharactor（CharacterBody2D）
├── AnimatedSprite2D
├── CollisionShape2D
├── Hitbox（Area2D）
│     └── CollisionShape2D
├── HurtSound（AudioStreamPlayer）
└── GunPivot（Node2D）            ← ✅ 刚刚添加
      ├── handgun（实例）         ← ✅ 拖入 handgun.tscn
      └── Muzzle（Node2D）        ← ✅ 枪口标记
```

<!-- 📷 图片：2D 视口中 GunPivot 的原点（小橙点）位于角色手部，handgun 贴图贴在枪的位置，Muzzle 节点在枪管末端 -->
![GunPivot 和 Muzzle 位置](images/ch4_gunpivot_muzzle.png)
*图 5-9：GunPivot 原点在握枪手部，Muzzle 标记枪管末端，子弹从 Muzzle 处生成*

**为什么需要这三个节点？**

- **`GunPivot`（Node2D）**：一个空节点，作为枪的旋转轴心。代码让它朝向鼠标，枪就会跟着旋转。
- **`handgun`（子场景实例）**：手枪的外观贴图，放在 `GunPivot` 下随其旋转。
- **`Muzzle`（Node2D）**：一个空节点，标记枪口位置。子弹生成时以它的全局坐标为起点，确保子弹从枪管末端飞出。

**操作步骤：**
1. 在文件系统中双击打开 `scenes/main_charactor.tscn`
2. 选中 `MainCharactor` 根节点，按 `Ctrl+A` 添加 `Node2D`，重命名为 `GunPivot`
3. 在 2D 视口中将 `GunPivot` 的 **Position 移到角色手部位置**
4. 选中 `GunPivot`，将文件系统中的 **`handgun.tscn` 拖入场景树**（作为实例子节点）
5. 选中 `GunPivot`，按 `Ctrl+A` 添加 `Node2D`，重命名为 `Muzzle`
6. 在 2D 视口中将 `Muzzle` 移动到枪管末端位置

<!-- 📷 图片：选中 GunPivot，检视器中 Position 属性显示已偏移到手部位置 -->
![GunPivot Position](images/ch4_gunpivot_position.png)
*图 5-10：GunPivot 的位置需对准角色握枪的手部*

**检视器关键属性（GunPivot）：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Position` | 角色手部坐标（如 `(8, 4)`） | 枪绕此点旋转，需根据角色贴图手动调整 |

**检视器关键属性（Muzzle）：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Position` | 枪管末端坐标（如 `(18, 0)`） | 子弹生成起点，X 轴沿枪管方向向外偏移 |

> 按 `Ctrl+S` 保存 `main_charactor.tscn`。

---

### 5.11 玩家脚本中的枪械控制

> 现在 `handgun.tscn` 和 `bullet.tscn` 都已完成，可以在玩家脚本（`main_charactor.gd`）中添加枪械射击逻辑。

**需要在 `main_charactor.gd` 中进行以下额外更改：**

#### 步骤 1：预加载子弹场景

在脚本文件顶部添加常量：

```gdscript
const BULLET_SCENE = preload("res://scenes/bullet.tscn")
```

> `preload` 在游戏启动时就加载资源，避免每次射击时才加载导致小卡顿。

#### 步骤 2：在 Input Map 添加 `shoot` 动作

进入 `Project` → `Project Settings` → `Input Map` 标签页，添加：

| 动作名 | 绑定按键 | 用途 |
|--------|----------|------|
| `shoot` | 鼠标左键 | 射击子弹 |

#### 步骤 3：在 `_physics_process` 中添加鼠标跟随和射击

在原有移动代码末尾，`move_and_slide()` 之后添加：

```gdscript
func _physics_process(delta: float) -> void:
    # ... 原有的 WASD 移动代码 ...
    move_and_slide()

    # 枪口朝向鼠标位置
    $GunPivot.look_at(get_global_mouse_position())

    # 鼠标左键射击
    if Input.is_action_just_pressed("shoot") and not is_dead and not is_dashing:
        _shoot($GunPivot)
```

> `look_at(position)` 让节点转向指定全局坐标点，这样 `GunPivot` 的本地 +X 方向始终指向鼠标。

> `is_action_just_pressed` 只在按下的那一帧触发（不是持续按下），防止连射。

#### 步骤 4：添加 `_shoot()` 函数

```gdscript
func _shoot(gun_pivot: Node2D) -> void:
    var bullet := BULLET_SCENE.instantiate()     # 从 bullet.tscn 创建一个新实例
    var dir := Vector2(cos(gun_pivot.global_rotation), sin(gun_pivot.global_rotation))
    bullet.direction      = dir          # 子弹方向 = 枪口朝向
    bullet.extra_velocity = velocity     # 叠加玩家当前速度（子弹不会因玩家奖速而变慢）
    get_tree().current_scene.add_child(bullet)   # 将子弹加入场景根节点
    bullet.global_position = $GunPivot/Muzzle.global_position  # 子弹从枪口位置出发
```

> 子弹加入 `current_scene`（场景根节点）而非玩家自身子节点，这样子弹不会随玩家一起移动。

---

### 5.12 完整场景确认（bullet.tscn）

<!-- 📷 图片：完整 bullet.tscn 场景树，展开所有节点 -->
![子弹完整场景树](images/ch5_bullet_tree_final.png)
*图 5-8：bullet.tscn 最终节点树，共 4 个节点*

```
bullet（Area2D）               ← 根节点，用于检测与史莱姆 Hitbox 重叠
├── AnimatedSprite2D           ← 子弹外观贴图
├── CollisionShape2D           ← 子弹碰撞体（细长形）
└── RayCast2D                  ← 前方射线，检测墙体
```

> 按 `Ctrl+S` 保存场景，进入下一节附加脚本。

---

### 5.13 附加脚本（bullet.gd）函数说明

**操作步骤：**
1. 选中根节点 `bullet`，右键 → `创建脚本`，保存为 `scripts/bullet.gd`

```gdscript
extends Area2D
```
继承 `Area2D`，可以接收 `area_entered` 信号以检测命中。

---

#### `_physics_process(delta)` — 子弹移动与墙体检测

```gdscript
func _physics_process(delta: float) -> void:
    # 每帧移动：基础方向速度 + 继承自玩家的额外速度（矢量叠加）
    position += (direction * SPEED + extra_velocity) * delta

    _lifetime -= delta
    if _lifetime <= 0.0:
        queue_free()   # 超时销毁（queue_free：安全删除节点，本帧结束后执行）
        return

    # RayCast2D 在每个物理帧自动更新，直接查询结果
    if $RayCast2D.is_colliding():
        var collider: Node2D = $RayCast2D.get_collider()
        # 不是玩家也不是史莱姆 → 视为墙壁 → 销毁子弹
        if not collider.is_in_group("player") and not collider.is_in_group("slimes"):
            queue_free()
```

---

#### `_on_area_entered(area)` — 命中史莱姆

```gdscript
func _on_area_entered(area: Area2D) -> void:
    # area 是进入子弹检测范围的 Area2D（即史莱姆的 Hitbox）
    var parent := area.get_parent()   # 获取 Hitbox 的父节点（史莱姆本体）
    if parent != null and parent.has_method("take_damage") and not parent.is_in_group("player"):
        parent.take_damage(DAMAGE)    # 对史莱姆造成伤害
        queue_free()                  # 子弹命中后销毁
```

> `has_method("take_damage")` 检查节点是否有该函数，避免对没有血量系统的节点调用导致报错。

> **💡 连接信号**：在检视器的 `Node` 标签页中找到 `area_entered` 信号，点击 `连接`，选择 `bullet` 节点自身，即可自动生成连接代码。

---

## 第六章：史莱姆敌人场景（slime.tscn）

> 史莱姆是本游戏的唯一敌人，它会追踪玩家、蓄力冲锋，并在受伤/死亡时播放音效和动画。本章将从空白场景出发，逐步搭建史莱姆的完整节点树。

**本章结束后，你将拥有如下完整节点树：**
```
slime（CharacterBody2D）
├── AnimatedSprite2D
├── CollisionShape2D
├── Hitbox（Area2D）
│   └── CollisionShape2D
├── HurtSound（AudioStreamPlayer）
└── JumpSound（AudioStreamPlayer）
```

---

### 6.1 创建空白场景文件

1. 在文件系统面板右键 `scenes/` → `新建场景`
2. 此时场景树完全空白——不含任何节点
3. 按 `Ctrl+S` 保存，命名为 `slime.tscn`

**当前场景树状态：**
```
（空）
```

---

### 6.2 步骤一：添加根节点 CharacterBody2D

**当前场景树状态：**
```
slime（CharacterBody2D）    ← ✅ 刚刚添加
```

<!-- 📷 图片：场景树中只有 slime 根节点，检视器显示 Collision Layer/Mask 设置 -->
![史莱姆根节点](images/ch6_root_node.png)
*图 6-1：CharacterBody2D 作为根节点，命名为 slime*

**为什么使用 CharacterBody2D？**

与玩家角色相同，史莱姆也需要物理移动（追踪、冲锋、碰墙反弹），使用 `CharacterBody2D` 可以调用 `move_and_slide()` 进行带碰撞的移动。

**操作步骤：**
1. 在场景树顶部点击 `其他节点`，搜索 `CharacterBody2D`，双击创建
2. 在场景树中将该节点重命名为 `slime`
3. 在检视器中设置碰撞层

**检视器关键属性：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Collision Layer` | 第 3 层（slime） | 史莱姆自身所在的层 |
| `Collision Mask` | 第 1 层（world）+ 第 2 层（player） | 与地图和玩家产生物理碰撞 |

---

### 6.3 步骤二：添加 AnimatedSprite2D（史莱姆动画）

**当前场景树状态：**
```
slime（CharacterBody2D）
└── AnimatedSprite2D        ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 AnimatedSprite2D，检视器中展开 Sprite Frames 面板，显示 idle/move/injury/death 四种动画 -->
![史莱姆动画帧](images/ch6_sprite_frames.png)
*图 6-2：史莱姆有 4 种动画，death 动画播完后节点将被删除*

**为什么需要 AnimatedSprite2D？**

史莱姆有多种行为状态（待机、移动、受伤、死亡），每种状态对应不同动画，`AnimatedSprite2D` 负责在这些动画之间切换。

**操作步骤：**
1. 选中 `slime` 根节点，按 `Ctrl+A` 添加 `AnimatedSprite2D`
2. 在检视器 `Sprite Frames` 中点击 `新建 SpriteFrames`，导入史莱姆图片
3. 创建以下动画并导入对应帧：

| 动画名 | 是否循环 | 说明 |
|--------|----------|------|
| `idle` | ✅ 循环 | 站立待机 |
| `move` | ✅ 循环 | 移动追踪 |
| `injury` | ❌ 不循环 | 受伤闪烁 |
| `death` | ❌ 不循环 | 死亡动画，播完后删除节点 |

> **💡 提示**：`death` 动画不循环，因为它播放完毕后脚本会调用 `queue_free()` 删除史莱姆节点。

---

### 6.4 步骤三：添加 CollisionShape2D（身体碰撞体）

**当前场景树状态：**
```
slime（CharacterBody2D）
├── AnimatedSprite2D
└── CollisionShape2D        ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 CollisionShape2D，2D 视口中显示圆形碰撞区覆盖在史莱姆贴图上，检视器显示 CircleShape2D 半径 -->
![史莱姆碰撞体](images/ch6_collision_shape.png)
*图 6-3：圆形碰撞体适合史莱姆这类圆滚滚的形状，移动时不容易卡角*

**为什么需要 CollisionShape2D？**

`CharacterBody2D` 自身没有碰撞体积，必须配合 `CollisionShape2D` 才能与地图墙壁发生物理碰撞，防止史莱姆穿墙。

**操作步骤：**
1. 选中 `slime` 根节点，按 `Ctrl+A` 添加 `CollisionShape2D`
2. 在检视器 `Shape` 下拉菜单中选择 `CircleShape2D`
3. 调整半径使碰撞圆略小于贴图，内缩 2~3 像素

**检视器关键属性：**

| 属性 | 建议值 | 说明 |
|------|--------|------|
| `Shape` | `CircleShape2D` | 圆形碰撞体，适合圆形敌人 |
| `Shape → Radius` | `约贴图半径 - 2px` | 略小于外观，避免视觉上"悬空"碰撞 |

---

### 6.5 步骤四：添加 Hitbox（Area2D + CollisionShape2D）

**当前场景树状态：**
```
slime（CharacterBody2D）
├── AnimatedSprite2D
├── CollisionShape2D
└── Hitbox（Area2D）        ← ✅ 刚刚添加
    └── CollisionShape2D   ← ✅ 同步添加
```

<!-- 📷 图片：选中 Hitbox 节点，检视器中 Monitoring = false，Monitorable = true -->
![史莱姆 Hitbox 检视器](images/ch6_hitbox_inspector.png)
*图 6-4：史莱姆 Hitbox 平时关闭（Monitoring = false），冲锋时由代码开启*

**为什么需要 Hitbox？**

史莱姆使用"冲锋攻击"机制：平时 `Hitbox` 关闭，不消耗性能；冲锋时由脚本打开，检测是否撞到玩家并造成伤害。

**操作步骤：**
1. 选中 `slime` 根节点，按 `Ctrl+A` 添加 `Area2D`，命名为 `Hitbox`
2. 选中 `Hitbox`，按 `Ctrl+A` 添加 `CollisionShape2D`，选择 `CircleShape2D`，半径与身体碰撞体相近
3. 在检视器中设置以下属性：

**检视器关键属性（Hitbox 节点）：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Monitoring` | `false` | 默认关闭检测，冲锋时由脚本开启 |
| `Monitorable` | `true` | 允许被子弹的 Area2D 检测到 |
| `Collision Layer` | 第 3 层（slime） | 子弹检测的目标层 |
| `Collision Mask` | 第 2 层（player） | 只检测玩家 |

---

### 6.6 步骤五：添加 HurtSound 和 JumpSound

**当前场景树状态：**
```
slime（CharacterBody2D）
├── AnimatedSprite2D
├── CollisionShape2D
├── Hitbox（Area2D）
│   └── CollisionShape2D
├── HurtSound（AudioStreamPlayer）  ← ✅ 刚刚添加
└── JumpSound（AudioStreamPlayer）  ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 HurtSound，检视器中 Stream 字段显示已导入的音效文件，Autoplay = false -->
![史莱姆音效节点](images/ch6_audio_nodes.png)
*图 6-5：两个音效节点分别负责受伤音效和冲锋音效*

**为什么需要两个音效节点？**

- `HurtSound`：史莱姆**受伤**时播放，由 `take_damage()` 函数调用
- `JumpSound`：史莱姆**冲锋蓄力**时播放，由 AI 状态机调用

使用两个独立的 `AudioStreamPlayer` 可以同时播放不同音效，互不干扰。

**操作步骤：**
1. 选中 `slime` 根节点，按 `Ctrl+A` 添加 `AudioStreamPlayer`，命名为 `HurtSound`
2. 再次添加一个 `AudioStreamPlayer`，命名为 `JumpSound`
3. 在检视器中为每个节点的 `Stream` 字段导入对应音效文件

**检视器关键属性（两个节点相同）：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Stream` | 导入对应 `.wav` / `.ogg` | 播放的音效文件 |
| `Autoplay` | `false` | 不自动播放，由脚本调用 `play()` |
| `Max Polyphony` | `1` | 同一时刻最多 1 个实例（避免叠音） |

---

### 6.7 完整场景确认（slime.tscn）

<!-- 📷 图片：完整 slime.tscn 场景树，展开所有节点 -->
![史莱姆完整场景树](images/ch6_slime_tree_final.png)
*图 6-6：slime.tscn 最终节点树，共 7 个节点（含 Hitbox 子节点）*

```
slime（CharacterBody2D）
├── AnimatedSprite2D               ← 史莱姆外观和动画
├── CollisionShape2D               ← 身体物理碰撞（圆形）
├── Hitbox（Area2D）               ← 攻击检测区域（冲锋时开启）
│   └── CollisionShape2D           ← Hitbox 的碰撞范围
├── HurtSound（AudioStreamPlayer） ← 受伤音效
└── JumpSound（AudioStreamPlayer） ← 冲锋蓄力音效
```

> 按 `Ctrl+S` 保存场景，进入下一节附加脚本。

---

### 6.8 附加脚本（slime.gd）函数说明

**操作步骤：**
1. 选中根节点 `slime`，右键 → `创建脚本`，保存为 `scripts/slime.gd`

```gdscript
extends CharacterBody2D
```
继承 `CharacterBody2D`，可以使用 `move_and_slide()` 进行带物理碰撞的移动。

---

#### `_ready()` — 初始化

```gdscript
func _ready() -> void:
    process_mode = PROCESS_MODE_PAUSABLE  # 游戏暂停时，该节点停止更新
    add_to_group("slimes")                # 加入 "slimes" 组，供场景管理查找
    player = get_parent().get_node("MainCharactor")  # 获取玩家节点引用
```

> `PROCESS_MODE_PAUSABLE`：当 `get_tree().paused = true` 时，该节点停止执行 `_process` 和 `_physics_process`。

---

#### `_physics_process(delta)` — AI 状态机

史莱姆的行为按优先级排列，每帧只执行一个状态：

```
优先级：死亡 > 冲锋 > 蓄力 > 攻击范围内 > 追踪（含碰墙反弹）
```

```gdscript
func _physics_process(delta: float) -> void:
    if is_dead: return      # 死亡后停止一切逻辑

    var to_player := player.global_position - global_position  # 指向玩家的向量
    var distance  := to_player.length()                        # 到玩家的距离

    if lunge_timer > 0.0:
        # ── 状态：冲锋 ──────────────────────────────────────
        velocity = lunge_direction * LUNGE_SPEED    # 高速冲向玩家
        # 检测 Hitbox 内是否有玩家的 Area2D
        for area in $Hitbox.get_overlapping_areas():
            if area.get_parent() == player:
                _attack_player()    # 命中则造成伤害
    elif is_winding_up:
        # ── 状态：蓄力 ──────────────────────────────────────
        velocity = velocity.lerp(Vector2.ZERO, ACCELERATION * delta)  # 减速停止
    elif distance <= ATTACK_RANGE:
        # ── 状态：攻击范围内 ────────────────────────────────
        velocity = velocity.lerp(Vector2.ZERO, ACCELERATION * delta)  # 停下等待
        if attack_timer <= 0.0:
            # 冷却好了就蓄力再冲
            lunge_direction = to_player.normalized()
            is_winding_up = true
    else:
        # ── 状态：追踪 ──────────────────────────────────────
        if bounce_timer > 0.0:
            velocity = velocity.lerp(bounce_direction * SPEED, ACCELERATION * delta)
        else:
            # 判断水平/垂直哪个方向更接近玩家，优先那个方向移动
            var target_vel: Vector2
            if abs(to_player.x) >= abs(to_player.y):
                target_vel = Vector2(sign(to_player.x), 0) * SPEED
            else:
                target_vel = Vector2(0, sign(to_player.y)) * SPEED
            velocity = velocity.lerp(target_vel, ACCELERATION * delta)

    move_and_slide()

    # 碰墙检测（仅追踪阶段）
    if is_on_wall() and bounce_timer <= 0.0 and not is_attacking:
        var normal := Vector2.ZERO
        for i in get_slide_collision_count():
            normal += get_slide_collision(i).get_normal()  # 累加所有碰撞面的法线
        bounce_direction = normal.normalized()  # 法线方向 = 远离墙壁的方向
        bounce_timer = BOUNCE_DURATION
```

> `lerp(a, b, t)` = 线性插值，每帧向目标靠近 `t` 比例。`ACCELERATION * delta` 让过渡速度与帧率无关。

---

#### `_die()` — 死亡处理

```gdscript
func _die() -> void:
    is_dead = true
    $CollisionShape2D.set_deferred("disabled", true)  # 禁用碰撞，其他物体不再能碰到它
    $Hitbox.monitoring  = false   # 停止主动检测
    $Hitbox.monitorable = false   # 不再被其他物体检测到（子弹穿过它）
    health_bar.hide()
    $AnimatedSprite2D.play("death")
    $AnimatedSprite2D.animation_finished.connect(queue_free)  # 动画播完后删除整个节点
```

> `animation_finished.connect(queue_free)` 将信号直接连接到函数，动画一结束就自动调用 `queue_free()`。

---

## 第七章：游戏主场景管理（main_scene.tscn）

> 主场景是游戏的"舞台"，负责把地图、玩家、敌人、UI 组装在一起，并通过脚本管理暂停、胜利、死亡等全局逻辑。本章将从空白场景出发，逐步搭建主场景的完整结构。

**本章结束后，你将拥有如下完整节点树：**
```
MainScene（Node2D）
├── ground（TileMapLayer）
├── MainCharactor（实例：main_charactor.tscn）
├── slime × N（实例：slime.tscn，可放置多个）
├── DeathScreen（CanvasLayer）
│   └── ...（按钮等 UI 节点）
├── PauseMenu（CanvasLayer）
│   └── ...（按钮、音量滑条等）
└── VictoryScreen（CanvasLayer）
    └── ...（提示文字、按钮等）
```

---

### 7.1 创建空白场景文件

1. 在文件系统面板右键 `scenes/` → `新建场景`
2. 此时场景树完全空白——不含任何节点
3. 按 `Ctrl+S` 保存，命名为 `main_scene.tscn`

**当前场景树状态：**
```
（空）
```

---

### 7.2 步骤一：添加根节点 Node2D

**当前场景树状态：**
```
MainScene（Node2D）    ← ✅ 刚刚添加
```

<!-- 📷 图片：场景树中只有 MainScene 根节点，检视器为空 -->
![主场景根节点](images/ch7_root_node.png)
*图 7-1：使用 Node2D 作为根节点，它是最轻量的 2D 节点，只负责组织子节点*

**为什么使用 Node2D？**

主场景只需作为"容器"承载所有子节点，不需要物理碰撞或动画能力。`Node2D` 是最轻量的 2D 节点，功能最少，开销最小。

**操作步骤：**
1. 在场景树顶部点击 `其他节点`，搜索 `Node2D`，双击创建
2. 将根节点重命名为 `MainScene`

---

### 7.3 步骤二：添加 TileMapLayer（地图）

**当前场景树状态：**
```
MainScene（Node2D）
└── ground（TileMapLayer）    ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 ground 节点，底部出现 TileMap 编辑工具栏，右侧 TileSet 面板显示可用瓦片 -->
![TileMap 编辑](images/ch7_tilemap_editor.png)
*图 7-2：TileMapLayer 的地图绘制界面，选中节点后底部会出现 TileMap 工具栏*

**为什么需要 TileMapLayer？**

`TileMapLayer` 用来绘制地面和墙壁，是游戏世界的基础。它将图块（瓦片）排布在网格上，并为每种瓦片配置物理碰撞形状，让玩家和史莱姆无法穿墙。

**操作步骤：**
1. 选中 `MainScene` 根节点，按 `Ctrl+A` 添加 `TileMapLayer`，命名为 `ground`
2. 在检视器中点击 `Tile Set` → `新建 TileSet`，然后导入地图图块图片
3. 选中 `ground` 节点后，底部会出现 TileMap 编辑工具栏，用画笔工具绘制地图

**检视器关键属性：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Collision Layer` | 第 1 层（world） | 地图所在物理层 |
| `Collision Mask` | 无需设置 | 地图不主动检测其他物体 |

> **重要**：地面和墙壁的碰撞必须在 TileSet 编辑器中为每种瓦片手动绘制碰撞形状，否则角色和子弹会穿过地图。

<!-- 📷 图片：TileSet 编辑器中，选中某个瓦片，显示其 Physics 标签页中绘制的碰撞多边形 -->
![TileSet 碰撞设置](images/ch7_tileset_collision.png)
*图 7-3：在 TileSet 编辑器中为瓦片添加物理碰撞形状*

---

### 7.4 步骤三：实例化玩家角色（MainCharactor）

**当前场景树状态：**
```
MainScene（Node2D）
├── ground（TileMapLayer）
└── MainCharactor（main_charactor.tscn 实例）  ← ✅ 刚刚添加
```

<!-- 📷 图片：文件系统中将 main_charactor.tscn 拖拽到 2D 视口，场景树中出现 MainCharactor 节点（带场景图标） -->
![实例化玩家](images/ch7_instanciate_player.png)
*图 7-4：将 main_charactor.tscn 拖入场景，场景树中显示实例节点（带小电影图标）*

**为什么是"实例化"而不是新建节点？**

玩家角色已经在第四章制作为独立场景（`main_charactor.tscn`）。在主场景中使用"实例化"引入它，可以保持场景独立性——修改 `main_charactor.tscn` 时，所有引用它的场景都会自动更新。

**操作步骤：**
1. 在文件系统面板找到 `scenes/main_charactor.tscn`
2. 将其**拖拽到 2D 视口**中，或按场景树顶部的"链接"图标 → 选择文件
3. 在 2D 视口中将玩家拖到地图的合适出生位置

---

### 7.5 步骤四：实例化史莱姆敌人

**当前场景树状态：**
```
MainScene（Node2D）
├── ground（TileMapLayer）
├── MainCharactor（main_charactor.tscn 实例）
└── slime（slime.tscn 实例，可多个）  ← ✅ 刚刚添加
```

<!-- 📷 图片：文件系统中将 slime.tscn 拖入视口，并在地图不同位置放置多只史莱姆实例 -->
![实例化史莱姆](images/ch7_instanciate_slime.png)
*图 7-5：可以多次拖入 slime.tscn，在地图上放置多只史莱姆*

**操作步骤：**
1. 在文件系统面板找到 `scenes/slime.tscn`
2. 将其拖拽到 2D 视口中，每次拖入会创建一个独立实例
3. 重复此操作，在地图上放置你想要的史莱姆数量

> **💡 提示**：每个史莱姆实例是独立的，它们共享同一份 `slime.tscn` 的节点结构，但各自的状态（位置、血量等）互相独立。

---

### 7.6 步骤五：添加 UI（CanvasLayer）

**当前场景树状态：**
```
MainScene（Node2D）
├── ground（TileMapLayer）
├── MainCharactor（main_charactor.tscn 实例）
├── slime × N（slime.tscn 实例）
├── DeathScreen（CanvasLayer）     ← ✅ 刚刚添加
├── PauseMenu（CanvasLayer）       ← ✅ 刚刚添加
└── VictoryScreen（CanvasLayer）   ← ✅ 刚刚添加
```

<!-- 📷 图片：选中 PauseMenu 节点（CanvasLayer），检视器中 Process Mode = Always，Layer = 1 -->
![CanvasLayer 设置](images/ch7_canvaslayer_inspector.png)
*图 7-6：CanvasLayer 的 Process Mode 必须设为 Always，才能在暂停时正常显示和响应按钮*

**为什么需要 CanvasLayer？**

`CanvasLayer` 是独立于游戏世界的 UI 层：
- **不随摄像机移动**：UI 始终固定在屏幕上
- **`Process Mode = ALWAYS`**：游戏暂停时 UI 仍可响应按钮点击

**操作步骤：**
1. 选中 `MainScene` 根节点，按 `Ctrl+A` 添加 `CanvasLayer`，命名为 `DeathScreen`
2. 同样的方法添加 `PauseMenu` 和 `VictoryScreen`
3. 选中每个 CanvasLayer，在检视器中将 `Process Mode` 设为 `Always`
4. 在每个 CanvasLayer 内部，按照设计添加按钮、标签等 UI 节点

**检视器关键属性（每个 CanvasLayer）：**

| 属性 | 值 | 说明 |
|------|-----|------|
| `Layer` | `1` | UI 层级，数字越大越靠前显示 |
| `Process Mode` | `Always` | 即使游戏暂停也继续处理输入 |

> 初始状态下，三个 UI 界面都应设为隐藏，由脚本在对应事件时显示。

---

### 7.7 完整场景确认（main_scene.tscn）

<!-- 📷 图片：完整 main_scene.tscn 场景树，展开所有顶层节点 -->
![主场景完整场景树](images/ch7_main_scene_tree.png)
*图 7-7：main_scene.tscn 最终节点树*

```
MainScene（Node2D）
├── ground（TileMapLayer）                  ← 地图层，绘制地面和墙壁
├── MainCharactor（main_charactor.tscn）   ← 玩家角色实例
├── slime × N（slime.tscn）               ← N 只史莱姆敌人实例
├── DeathScreen（CanvasLayer）             ← 死亡界面
├── PauseMenu（CanvasLayer）               ← 暂停菜单
└── VictoryScreen（CanvasLayer）           ← 胜利界面
```

> 按 `Ctrl+S` 保存场景，进入下一节附加脚本。

---

### 7.8 附加脚本（main_scene.gd）函数说明

**操作步骤：**
1. 选中根节点 `MainScene`，右键 → `创建脚本`，保存为 `scripts/main_scene.gd`

```gdscript
extends Node2D
```

---

#### `_ready()` — 场景初始化

```gdscript
func _ready() -> void:
    # 将游戏节点设为可暂停（暂停时停止运行）
    $MainCharactor.process_mode = PROCESS_MODE_PAUSABLE

    # 初始时隐藏所有覆盖界面
    $DeathScreen.hide()
    $PauseMenu.hide()
    $VictoryScreen.hide()

    # 连接玩家死亡信号
    $MainCharactor.died.connect(_on_player_died)

    # 延迟连接（确保场景中所有节点的 _ready 都已执行完毕）
    call_deferred("_connect_coins")
    call_deferred("_connect_slimes")
```

> `call_deferred("函数名")` 将函数调用推迟到本帧结束后执行，确保场景中所有节点完成初始化。

---

#### `_unhandled_input(event)` — 暂停键处理

```gdscript
func _unhandled_input(event: InputEvent) -> void:
    if event.is_action_pressed("pause"):
        if $PauseMenu.visible:
            _resume()                    # 已暂停 → 恢复游戏
        elif not $DeathScreen.visible and not $VictoryScreen.visible:
            _show_pause_menu()           # 正常游戏中 → 显示暂停菜单
```

> `_unhandled_input` 与 `_input` 的区别：前者只在事件未被 UI 控件（如按钮）处理时才触发，避免按钮点击被误判为暂停。

---

#### `_process(delta)` — 长按 R 重置

```gdscript
func _process(delta: float) -> void:
    if Input.is_action_pressed("reset"):
        reset_hold_timer += delta            # 累积按下时间
        $CoinHUD/ResetBar.value = reset_hold_timer  # 更新进度条显示
        if reset_hold_timer >= 1.0:          # 按满 1 秒
            get_tree().paused = false
            get_tree().reload_current_scene()  # 重载整个场景（重置游戏）
    else:
        reset_hold_timer = 0.0               # 没按则清零
        $CoinHUD/ResetBar.hide()
```

---

#### `_on_slime_removed()` — 胜利检测

```gdscript
func _on_slime_removed() -> void:
    if not is_inside_tree(): return   # 场景正在卸载时跳过，防止访问已销毁节点
    if get_tree().get_nodes_in_group("slimes").is_empty():
        _show_victory()               # 所有史莱姆都死亡 → 胜利
```

> 每只史莱姆死亡销毁时会触发 `tree_exited` 信号，该函数在 `_ready` 中通过 `_connect_slimes()` 与此信号连接。

---

#### `_on_volume_changed(value)` — 音量调节

```gdscript
func _on_volume_changed(value: float) -> void:
    # 将滑块线性值（0.0 ~ 1.0）转为分贝（dB）
    var db := linear_to_db(value) if value > 0.0 else -80.0
    AudioServer.set_bus_volume_db(0, db)   # 设置主音频总线音量
```

> 人耳对音量的感知是对数关系，`linear_to_db()` 完成线性→对数的转换，使滑块拖动感受更自然。

---

### 7.9 设置游戏主场景

所有场景和功能制作完成后，需要告诉 Godot 游戏**从哪个场景开始运行**（即按 `F5` 时启动哪个场景）。

<!-- 📷 图片：Project Settings → Application → Run，Main Scene 字段显示 start.tscn 路径 -->
![设置主场景](images/ch7_main_scene.png)
*图 7-8：项目启动时自动打开的场景（Main Scene）*

**操作步骤：**
1. 菜单栏点击 `Project` → `Project Settings`
2. 左侧找到 `Application` → `Run`
3. 点击 `Main Scene` 字段右侧的文件夹图标，选择 `scenes/start.tscn`

设置完成后，按 `F5` 运行项目将从开始菜单（`start.tscn`）启动，而非直接进入游戏场景。

---

# 附录 A：Godot 编辑器快捷键速查

### A.1 运行与调试

| 快捷键 | 功能 |
|--------|------|
| `F5` | 运行整个项目（从主场景开始） |
| `F6` | 运行当前打开的场景 |
| `F7` | 单步暂停（调试模式下） |
| `F8` | 停止运行游戏 |

---

### A.2 场景与文件操作

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+S` | 保存当前文件/场景 |
| `Ctrl+Shift+S` | 场景另存为 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |
| `Ctrl+C` | 复制选中节点 |
| `Ctrl+V` | 粘贴节点 |
| `Ctrl+D` | 原地复制（Duplicate）选中节点 |
| `Ctrl+X` | 剪切节点 |
| `Delete` | 删除选中节点 |
| `Ctrl+A` | 向当前选中节点添加子节点 |
| `Ctrl+Shift+A` | 在选中节点的同级添加节点 |

---

### A.3 2D 视口操作

| 快捷键 | 功能 |
|--------|------|
| `Q` | 选择工具（点选/框选节点） |
| `W` | 移动工具（拖动节点位置） |
| `E` | 旋转工具 |
| `S` | 缩放工具 |
| `R` | 标尺/矩形工具（TileMap 绘制时） |
| `空格 + 拖动` | 平移视口画布 |
| `鼠标滚轮` | 缩放视口 |
| `Ctrl+滚轮` | 精细缩放视口 |
| `数字键 1` | 将视口缩放重置为 100% |
| `Ctrl+Shift+F` | 聚焦到选中节点（使节点居中显示） |

---

### A.4 脚本编辑器

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+F` | 在当前脚本中搜索 |
| `Ctrl+H` | 搜索并替换 |
| `Ctrl+G` | 跳转到指定行号 |
| `Ctrl+/` | 注释/取消注释选中行 |
| `Tab` | 增加缩进 |
| `Shift+Tab` | 减少缩进 |
| `Ctrl+Space` | 触发代码补全 |
| `Ctrl+Click` | 跳转到函数/变量定义处 |
| `Alt+Up/Down` | 将当前行上移/下移 |
| `Ctrl+Shift+C` | 复制选中行（不剪切原行） |
| `F1` 或 `Ctrl+F1` | 查看选中关键字的官方文档 |

---

### A.5 场景树操作

| 快捷键 | 功能 |
|--------|------|
| `F2` | 重命名选中节点 |
| `Ctrl+↑ / Ctrl+↓` | 在兄弟节点间移动（调整顺序） |
| `Alt+Click` 展开箭头 | 递归展开/折叠子树 |

---

## 附录 B：Windows 系统开发常用快捷键

### B.1 文件与窗口管理

| 快捷键 | 功能 |
|--------|------|
| `Win+E` | 打开文件资源管理器 |
| `Win+D` | 显示桌面（最小化所有窗口） |
| `Alt+Tab` | 在打开的应用程序间切换 |
| `Win+Tab` | 任务视图（查看所有打开窗口） |
| `Win+←/→` | 将当前窗口贴靠到屏幕左/右半边 |
| `Win+↑` | 最大化当前窗口 |
| `Win+↓` | 还原/最小化当前窗口 |
| `Alt+F4` | 关闭当前窗口/程序 |
| `Ctrl+Shift+Esc` | 直接打开任务管理器（查看 CPU/内存占用） |

---

### B.2 文件管理器操作

| 快捷键 | 功能 |
|--------|------|
| `F2` | 重命名选中文件/文件夹 |
| `F5` | 刷新文件列表 |
| `Ctrl+N` | 新建文件夹 |
| `Ctrl+A` | 全选 |
| `Ctrl+Z` | 撤销文件操作（如误删可恢复） |
| `Delete` | 移入回收站 |
| `Shift+Delete` | 永久删除（不经过回收站，谨慎使用） |
| `Alt+←` | 返回上一级目录 |
| `Alt+D` | 选中地址栏（可直接输入路径） |
| `Ctrl+Shift+N` | 新建文件夹 |

---

### B.3 截图（制作教程时常用）

| 快捷键 | 功能 |
|--------|------|
| `Win+Shift+S` | 截取屏幕选定区域（截图到剪贴板） |
| `PrtSc` | 截取整个屏幕到剪贴板 |
| `Alt+PrtSc` | 截取当前窗口到剪贴板 |
| `Win+G` | 打开 Xbox Game Bar（可录制游戏画面） |
| `Win+Alt+R` | 开始/停止游戏录制（Game Bar 录制） |

> **💡 制作教程截图推荐流程**：
> 1. 在 Godot 中摆好需要截图的界面
> 2. 按 `Win+Shift+S` 框选区域截图
> 3. 打开画图/截图工具（`Win+S` 搜索"截图工具"）粘贴并保存为 PNG
> 4. 保存到项目的 `images/` 文件夹，替换对应的占位图片路径

---

### B.4 文本与剪贴板

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+C` | 复制 |
| `Ctrl+X` | 剪切 |
| `Ctrl+V` | 粘贴 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+A` | 全选 |
| `Ctrl+F` | 在当前程序中搜索 |
| `Win+V` | 打开剪贴板历史（可粘贴最近复制的多条内容） |

> **💡 提示**：`Win+V` 的剪贴板历史默认可能未开启，首次使用时按照提示打开即可。

---

*本教程基于项目现有实现编写。如有功能更新，对应章节的图片和说明请同步修改。*
